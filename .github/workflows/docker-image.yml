name: Build, Push and Deploy AI Service

on:
  push:
    branches: [ master ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ master ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Docker Hub Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Generate Docker Metadata
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: davidnguyen7603/coolchatai
          tags: |
            type=sha,format=short
            type=semver,pattern={{version}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and Push Image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push  # Chờ job build-and-push hoàn tất
    if: ${{ github.event_name != 'pull_request' && (github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/tags/')) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Render
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
          IMAGE_TAG: davidnguyen7603/coolchatai:latest,davidnguyen7603/coolchatai:sha-${{ github.sha }}
        run: |
          # Nếu là git tag, chọn tag version (ví dụ: v1.0.0)
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            FULL_TAG=$(echo "$IMAGE_TAG" | grep -o 'v[0-9]\+\.[0-9]\+\.[0-9]\+' || echo "")
          # Nếu là branch master, dùng tag 'latest'
          elif [[ "${{ github.ref }}" == refs/heads/master ]]; then
            FULL_TAG="latest"
          fi
          # Nếu không tìm thấy tag phù hợp, dùng tag đầu tiên (SHA)
          if [ -z "$FULL_TAG" ]; then
            FULL_TAG=$(echo "$IMAGE_TAG" | cut -d ',' -f 1)
          fi
          # Kiểm tra xem image đã sẵn sàng trên Docker Hub
          echo "Checking if image davidnguyen7603/coolchatai:$FULL_TAG is available..."
          until docker pull davidnguyen7603/coolchatai:$FULL_TAG > /dev/null 2>&1; do
            echo "Image not yet available, waiting 5 seconds..."
            sleep 5
          done
          echo "Image is available, proceeding with deployment..."
          # Gọi API Render để deploy
          RESPONSE=$(curl -s -o response.txt -w "%{http_code}" -X POST "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys" \
            -H "Authorization: Bearer $RENDER_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{"imageRef": "davidnguyen7603/coolchatai:'"$FULL_TAG"'","clearCache": true}')
          if [ "$RESPONSE" -ne 201 ]; then
            echo "❌ Deploy failed with status code: $RESPONSE"
            cat response.txt
            exit 1
          fi
          echo "✅ Triggered deploy image: davidnguyen7603/coolchatai:$FULL_TAG"